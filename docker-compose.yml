services:
  mongodb:
    image: mongo:7
    container_name: librotech
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: librotech
      MONGO_INITDB_ROOT_PASSWORD: librotech
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
